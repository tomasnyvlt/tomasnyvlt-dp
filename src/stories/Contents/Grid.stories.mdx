import { Meta, Story, Canvas, ArgsTable } from '@storybook/blocks';
import { DataDrivenForm } from '@src/index';
import { StorybookProviderDecorator } from '@src/StorybookProviderDecorator';
import Grid from '@src/components/contents/Grid';
import { getFieldWrapper } from '@src/stories/getFieldWrapper';

<Meta
  title="Contents / Grid"
  component={DataDrivenForm}
  decorators={[StorybookProviderDecorator]}
  tags={['autodocs']}
/>

# Grid

<Canvas>
  <Story name="Grid">
    {() => (
      <DataDrivenForm
        schema={getFieldWrapper({
          title: 'Grid',
          sectionContent: [
            {
              component: 'section',
              name: 'storybook.Grid',
              css: {
                mt: '3rem',
              },
              fields: [
                {
                  component: 'grid',
                  name: 'Grid',
                  fields: [
                    {
                      component: 'box',
                      name: 'Box1',
                      css: { w: "100%", h: "100px", bg: "red"}
                    },
                    {
                      component: 'box',
                      name: 'Box2',
                      css: { w: "100%", h: "100px", bg: "red"}
                    },
                    {
                      component: 'box',
                      name: 'Box3',
                      css: { w: "100%", h: "100px", bg: "blue", gridColumn: { sm: "1 / -1" },}
                    },
                  ]
                },
              ],
            },
          ],
        })}
      />
    )}
  </Story>
</Canvas>

## Component Props

<ArgsTable of={Grid} />
