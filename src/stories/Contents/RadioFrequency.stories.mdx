import { Meta, Story, Canvas, ArgsTable } from '@storybook/blocks';
import { DataDrivenForm } from '@src/index';
import { StorybookProviderDecorator } from '@src/StorybookProviderDecorator';
import RadioContainer from '@src/components/contents/RadioFrequency/RadioContainer';
import { getFieldWrapper } from '@src/stories/getFieldWrapper';

<Meta
  title="Contents / RadioFrequency"
  component={DataDrivenForm}
  decorators={[StorybookProviderDecorator]}
  tags={['autodocs']}
/>

# RadioFrequency

<Canvas>
  <Story name="RadioFrequency">
    {() => (
      <DataDrivenForm
        schema={getFieldWrapper({
          title: 'RadioFrequency',
          sectionContent: [
            {
              component: 'radio-container',
              name: 'Radiocontainer',
              options: [
                {
                  component: 'radio-frequency-button',
                  label: 'Ročně',
                  value: '2',
                  frequency: '1',
                  price: '2222',
                },
                {
                  component: 'radio-frequency-button',
                  label: 'Měsíčně',
                  value: 'monthly',
                  price: '1122',
                  frequency: '12',
                },
              ],
            },
          ],
        })}
      />
    )}
  </Story>
</Canvas>

## Component Props

<ArgsTable of={RadioContainer} />
